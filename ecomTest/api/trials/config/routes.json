{
  "routes": [
    {
      "method": "GET",
      "path": "/trials",
      "handler": "trials.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/trials/count",
      "handler": "trials.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/trials/:id",
      "handler": "trials.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/trials",
      "handler": "trials.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/trials/:id",
      "handler": "trials.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/trials/:id",
      "handler": "trials.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
